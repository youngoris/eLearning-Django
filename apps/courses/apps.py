from django.apps import AppConfig

# Define the configuration class for the 'courses' app
class CoursesConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.courses"
